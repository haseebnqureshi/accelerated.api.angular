(function() {

	/*
	Example Service
	*/

	// window.app.factory('example', ['accAuthAjax', function(ajax) {
	//
	// 	var that = this;
	//
	// 	this.getAll = function(success, error, done) {
	// 		ajax.get('/example', success || null, error || null, done || null);
	// 	};
	//
	// 	this.post = function(data, success, error, done) {
	// 		ajax.post('/example', 
	// 			data, success || null, error || null, done || null);
	// 	};
	//
	// 	this.get = function(id, success, error, done) {
	// 		ajax.get('/example' + '/' + id, success || null, error || null, done || null);
	// 	};
	//
	// 	this.put = function(id, data, success, error, done) {
	// 		ajax.put('/example' + '/' + id,
	// 			data, success || null, error || null, done || null);
	// 	};
	//
	// 	this.delete = function(id, success, error, done) {
	// 		ajax.delete('/example' + '/' + id, success || null, error || null, done || null);
	// 	};
	//
	// 	return this;	
	//		
	// }]);


})();